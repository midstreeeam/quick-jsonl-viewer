{
  "name": "quick-jsonl-viewer",
  "displayName": "Quick JSONL Viewer",
  "description": "View and preview large JSONL files in a readonly, formatted VS Code custom editor.",
  "version": "0.1.0",
  "publisher": "jsh9",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/jsh9/quick-jsonl-viewer.git"
  },
  "bugs": {
    "url": "https://github.com/jsh9/quick-jsonl-viewer/issues"
  },
  "homepage": "https://github.com/jsh9/quick-jsonl-viewer#readme",
  "keywords": [
    "jsonl",
    "json-lines",
    "json-lines-viewer",
    "large-files",
    "custom-editor"
  ],
  "galleryBanner": {
    "color": "#1f6feb",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.120.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/src/extension.js",
  "activationEvents": [
    "onCustomEditor:quickJsonlViewer.viewer",
    "onCommand:quickJsonlViewer.openCurrentFile",
    "onCommand:quickJsonlViewer.openSampleFiles"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    },
    "virtualWorkspaces": false
  },
  "contributes": {
    "commands": [
      {
        "command": "quickJsonlViewer.openCurrentFile",
        "title": "Open in Quick JSONL Viewer",
        "category": "Quick JSONL Viewer"
      }
    ],
    "languages": [
      {
        "id": "jsonl",
        "aliases": [
          "JSON Lines",
          "jsonl"
        ],
        "extensions": [
          ".jsonl"
        ]
      }
    ],
    "customEditors": [
      {
        "viewType": "quickJsonlViewer.viewer",
        "displayName": "Quick JSONL Viewer",
        "selector": [
          {
            "filenamePattern": "*.jsonl"
          },
          {
            "filenamePattern": "**/*.jsonl"
          }
        ],
        "priority": "default"
      }
    ],
    "configurationDefaults": {
      "workbench.editorAssociations": {
        "*.jsonl": "quickJsonlViewer.viewer"
      },
      "workbench.diffEditorAssociations": {
        "*.jsonl": "default"
      }
    },
    "configuration": {
      "title": "Quick JSONL Viewer",
      "properties": {
        "quickJsonlViewer.maxLines": {
          "type": "integer",
          "default": 20,
          "minimum": 0,
          "description": "Number of JSONL lines to load for display. Set to 0 to index the full file and render visible rows on demand."
        },
        "quickJsonlViewer.indent": {
          "type": "integer",
          "default": 2,
          "minimum": 1,
          "description": "Number of spaces used to indent JSON in Pretty print mode."
        },
        "quickJsonlViewer.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Automatically refresh open JSONL viewers when the underlying file changes. Disable to refresh manually from the viewer toolbar."
        },
        "quickJsonlViewer.indentGuides": {
          "type": "boolean",
          "default": true,
          "description": "Show vertical indentation guides in Pretty print mode."
        },
        "quickJsonlViewer.maxRenderedRowBytes": {
          "type": "integer",
          "default": 1048576,
          "minimum": 1,
          "description": "Maximum JSONL row size, in bytes, that Quick JSONL Viewer will parse, format, and render. Larger rows are shown as oversized previews."
        },
        "quickJsonlViewer.oversizedRowPreviewBytes": {
          "type": "integer",
          "default": 4096,
          "minimum": 0,
          "description": "Number of leading bytes to show for JSONL rows that exceed the max rendered row size. Set to 0 to hide oversized row previews."
        }
      }
    },
    "menus": {
      "commandPalette": [
        {
          "command": "quickJsonlViewer.openCurrentFile",
          "when": "!isInDiffEditor"
        }
      ],
      "editor/title": [
        {
          "command": "quickJsonlViewer.openCurrentFile",
          "when": "resourceScheme == file && resourceExtname == .jsonl && !isInDiffEditor",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "quickJsonlViewer.openCurrentFile",
          "when": "resourceScheme == file && resourceExtname == .jsonl",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "compile": "npm run compile:extension && npm run typecheck:webview && npm run build:webview",
    "compile:extension": "tsc -p ./",
    "typecheck:webview": "tsc -p tsconfig.webview.json --noEmit",
    "build:webview": "node scripts/build-webview.cjs",
    "format": "prettier . --write --ignore-unknown",
    "format:check": "prettier . --check --ignore-unknown",
    "prepare": "husky",
    "test": "npm run format:check && npm run compile && npm run test:coverage",
    "test:coverage": "c8 --all --src out/src --include 'out/src/**/*.js' --check-coverage --lines 95 --branches 95 --functions 95 npm run test:unit",
    "test:unit": "node scripts/run-tests.cjs",
    "test:vscode": "npm run compile && vscode-test",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.14.8",
    "@types/vscode": "1.120.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^3.0.0",
    "c8": "^11.0.0",
    "esbuild": "^0.28.1",
    "husky": "^9.1.7",
    "mocha": "^11.7.6",
    "prettier": "^3.8.4",
    "typescript": "^5.8.3"
  }
}
